{
  "name": "tarrot",
  "version": "2.0.0",
  "description": "小小问题 — a gentle daily tarot practice with AI-powered reading insight",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=development concurrently -k -n api,web -c blue,green \"npm run server\" \"vite\"",
    "dev:web": "vite",
    "dev:api": "NODE_ENV=development npm run server",
    "build": "vite build",
    "type-check": "vue-tsc --noEmit",
    "test": "node --test",
    "preview": "vite preview",
    "server": "node server/server.js",
    "start": "npm run dev",
    "prod": "npm run build && NODE_ENV=production npm run server"
  },
  "license": "MIT",
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "concurrently": "^10.0.5",
    "typescript": "~5.9.3",
    "vite": "^8.2.2",
    "vue-tsc": "^3.3.11"
  },
  "dependencies": {
    "vue": "^3.5.42"
  }
}
